# Fail2ban filter for nginx rate limiting
# Save as /etc/fail2ban/filter.d/nginx-rate-limit.conf

[Definition]
failregex = limiting requests, excess: \S+ by zone "\S+", client: <HOST>
            limiting connections by zone "\S+", client: <HOST>

ignoreregex =

[INCLUDES]
before = common.conf